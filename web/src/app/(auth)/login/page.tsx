import { LoginForm } from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Login Portal — Aetherbound Life RPG',
};

export default function LoginPage() {
  return <LoginForm />;
}
